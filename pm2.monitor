#!/bin/bash
APP_NAME="cogs-demo-monitor"
APP_PATH=`pwd`

DATA_DIR=/opt/cogs
LOG_DIR=/opt/cogs/log

INFO_LOG=$LOG_DIR/info.log
ERROR_LOG=$LOG_DIR/error.log
PID_FILE=$APP_DIR/$APP_NAME.pid

pm2 delete $APP_NAME
pm2 start $APP_PATH/monitor.js --name $APP_NAME -o $INFO_LOG -e $ERROR_LOG -p $PID_FILE
